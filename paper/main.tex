\documentclass{article}

% Package imports
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{cite}
\usepackage{enumitem}
\usepackage{xcolor}

% Theorem environments
\newtheorem{proposition}{Proposition}
\newtheorem{claim}{Claim}
\newtheorem{definition}{Definition}

% Metadata
\title{Structural Fingerprints of Label Memorization in Shallow ReLU Networks}

\author{
  Shamique Khan \\
  \texttt{shamique.khan@example.com} \\
  Organization
}

\begin{document}

\maketitle

% ============================================================
\begin{abstract}
Deep neural networks can memorize randomly labeled training examples while still generalizing well on clean test data. Despite extensive empirical documentation of this phenomenon, the mechanistic question of what structural traces memorization leaves inside the network remains largely unanswered. We present a systematic, four-phase analysis combining spectral geometry, centered kernel alignment (CKA), influence functions, and rank-one model editing (ROME) to characterize how label memorization manifests in shallow ReLU networks, validated across MNIST (two-layer MLP) and CIFAR-10 (three-layer MLP). Our central finding is that memorization distortion localizes at the deepest pre-output interface: on MNIST, CKA similarity at the single ReLU nonlinearity drops significantly ($\Delta = +0.160$, $p < 0.001$); on CIFAR-10, distortion shifts to the output-adjacent linear layer ($\Delta = +0.103$, $p = 0.009$). We further show that (i) spectral geometry changes are depth-dependent; (ii) ROME rank-one edit delta-norms serve as a robust cross-architecture geometric probe — significantly smaller under corruption across all 10 classes on both MNIST (fc1: 2.02$\times$, fc2: 4.34$\times$, all $p < 0.0001$) and CIFAR-10 (fc3: 1.84$\times$, all $p < 0.05$), scaling monotonically with noise rate (3.37--5.98$\times$), outperforming both spectral norms (1.86$\times$) and linear probes (AUC 0.514), with random rank-1 edits recovering 0\% of accuracy versus ROME's 10--22\%; (iii) sequential multi-layer ROME (fc2$\to$fc1) improves recovery by approximately 50\% over single-layer edits; and (iv) wider networks distribute memorized associations across more neurons, reducing monosemanticity and rendering individual circuit elements less specialized.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:introduction}

Deep neural networks have demonstrated a remarkable capacity to fit random labels — achieving near-zero training error on completely corrupted training sets while maintaining reasonable test accuracy on clean data \cite{zhang2017understanding}. This phenomenon raises a fundamental question: what structural changes does label memorization induce inside the network, and can these changes be systematically characterized?

Prior work has addressed memorization primarily through the lens of generalization bounds \cite{neyshabur2017exploring, arora2018stronger}, sample-level memorization scores \cite{feldman2020what, carlini2022quantifying}, and the necessity of memorization for long-tail learning \cite{feldman2020does}. However, a unified structural account — one that traces how memorization reshapes weight geometry, hidden representations, and causal intervention responses — remains absent.

In this paper, we provide such an account through four complementary analyses on a controlled two-layer ReLU network, validated on a three-layer CIFAR-10 MLP:

\begin{enumerate}[leftmargin=*]
  \item \textbf{Weight geometry (Phase 1):} Label corruption reshapes the singular value spectrum in a depth-dependent manner — shallow-layer spectral norms decrease, while deeper layers in multi-layer networks intensify to absorb additional separability burden.
  \item \textbf{Representation similarity (Phase 2):} CKA analysis reveals that memorization distortion concentrates at the deepest pre-output interface: the ReLU nonlinearity on MNIST ($\Delta = +0.160$, $p < 0.001$) and the output linear layer on CIFAR-10 ($\Delta = +0.103$, $p = 0.009$).
  \item \textbf{Influence functions (Phase 3):} A non-circular loss-based analysis shows corrupted samples are reliably harder to predict than clean ones, consistent with ground-truth corruption indices.
  \item \textbf{Causal intervention (Phase 4):} ROME rank-one edits detect memorization: corrupted models require smaller delta-norms across all 10 classes on both MNIST (fc1: 2.02$\times$, fc2: 4.34$\times$, all $p < 0.0001$) and CIFAR-10 (fc3: 1.84$\times$, all $p < 0.05$), random rank-1 edits recover 0\% of accuracy versus ROME's 10--22\%, and sequential multi-layer ROME (fc2$\to$fc1) improves recovery by approximately 50\% over single-layer. Multi-class recovery is positive in all 4 targeted corruption configurations.
\end{enumerate}

Each finding is validated across 10 seeds (MNIST) or 5 seeds (CIFAR-10) with 95\% confidence intervals, paired t-tests, and the cross-architecture comparison. Our scaling analysis further shows that wider networks distribute memorization across more neurons, reducing monosemanticity and aligning with the superposition hypothesis \cite{elhage2022toy}.

% ============================================================
\section{Related Work}
\label{sec:related}

\textbf{Memorization and generalization.} Zhang et al. \cite{zhang2017understanding} first demonstrated that deep networks can memorize random labels while generalizing, challenging conventional generalization theory. Feldman \cite{feldman2020does} showed that memorization is necessary for learning from long-tail distributions, and Feldman \& Zhang \cite{feldman2020what} provided per-sample memorization scores via influence estimation. Carlini et al. \cite{carlini2022quantifying} quantified memorization in language models through extraction attacks. Our work differs in adopting a structural, mechanism-level definition of memorization — we ask not \emph{whether} memorization occurs but \emph{what geometric traces it leaves}.

\textbf{Representation analysis tools.} Kornblith et al. \cite{kornblith2019similarity} introduced centered kernel alignment (CKA) for comparing neural network representations, and Nguyen et al. \cite{nguyen2021wide} applied it across network families. Raghu et al. \cite{raghu2017svcca} developed SVCCA for analyzing representation dynamics. We use linear CKA specifically for its invariance to orthogonal transformations, which is essential for comparing clean and corrupted models whose weights may rotate. Koh \& Liang \cite{koh2017understanding} introduced influence functions for understanding black-box predictions; we adapt their conjugate gradient approach to a memorization setting with ground-truth corruption labels.

\textbf{Model editing and mechanistic interpretability.} Meng et al. \cite{meng2022locating} proposed ROME (Rank-One Model Editing) for modifying factual associations in GPT-style models. Mitchell et al. \cite{mitchell2022memory} introduced a related memory-based editing approach. We adapt the ROME rank-one update formula to the setting of classification memorization detection — applying edits to the output layer of a shallow network and using the delta-norm as a geometric separability probe. Elhage et al. \cite{elhage2022toy} proposed the superposition hypothesis, where models represent more features than neurons; our monosemanticity scaling results directly engage with this hypothesis. Olah et al. \cite{olah2020zoom} introduced circuits as a framework for mechanistic interpretability; our circuit sparsity analysis provides a controlled setting for circuit discovery.

\textbf{Gap.} No prior work has jointly measured spectral geometry, representation similarity, influence functions, and causal intervention localizability across the same controlled memorization setting, nor applied ROME-style edits as a memorization detection mechanism in classification networks.

% ============================================================
\section{Theoretical Motivation}
\label{sec:theory}

We motivate our empirical analysis with three theoretical propositions that formalize why memorization should produce detectable structural signatures. Each proposition is supported by a corresponding experiment.

\begin{proposition}[Deepest pre-output interface localization]
\label{prop:nonlinearity}
In a ReLU network trained with cross-entropy loss, random label noise forces the activation pattern at the deepest nonlinear interface before the output to absorb class boundary inconsistencies. The depth of this interface scales with network depth.
\end{proposition}

\textit{Argument.} Linear layers $W_1$, $W_2$, etc.\ can only rotate and scale the representation; they cannot change the topology of the decision boundary. Each ReLU nonlinearity partitions the input space into polyhedral regions and determines which neurons activate. When labels are randomly flipped, the model must accommodate inputs from the same polyhedral region being assigned to different classes. The deepest pre-output interface bears the brunt of this disambiguation because it is the final stage before the output layer's decision boundary. In a two-layer network this interface is the single ReLU; in deeper networks it shifts to later layers. \textbf{Supporting experiment:} Phase 2 (Section~\ref{sec:phase2}) — MNIST shows $\Delta=+0.160$ at the single nonlinearity, CIFAR-10 shows $\Delta=+0.103$ at the output-adjacent interface.

\begin{proposition}[Depth-dependent spectral reshaping]
\label{prop:spectral}
Label corruption reshapes the singular value spectrum in a depth-dependent manner: shallow layers flatten (lower spectral norms), while deeper layers may intensify to absorb additional separability burden.
\end{proposition}

\textit{Argument.} In a shallow network, corrupted labels force weight matrices to distribute capacity more uniformly, flattening the spectrum and reducing effective rank. In deeper networks, intermediate and output layers must absorb additional class-boundary complexity, which can increase their leading singular values even as the effective rank (number of significant directions) falls. \textbf{Supporting experiment:} Phase 1 (Section~\ref{sec:phase1}) — MNIST FC1 and FC2 both decrease; CIFAR-10 FC1 is flat while FC2 and FC3 increase.

\begin{proposition}[Edit magnitude as separability probe]
\label{prop:rome}
The ROME delta-norm $\|\Delta\| = \|(v - Wu) \otimes u / (u \cdot u)\|$ measures class boundary overlap in hidden space: larger edits correspond to more orthogonal class boundaries, smaller edits to overlapping ones.
\end{proposition}

\textit{Argument.} In clean models, orthogonally separated classes require a larger perturbation to change their prediction. In corrupted models, overlapping class boundaries mean a smaller perturbation suffices. \textbf{Supporting experiment:} Phase 4 (Section~\ref{sec:phase4}).

% ============================================================
\section{Experimental Setup}
\label{sec:setup}

\textbf{Architecture.} Our primary model is a two-layer ReLU network (MNISTNet) with architecture $784 \to 16 \to 10$, chosen for full interpretability. For cross-validation, we use a three-layer MLP (CIFAR-10 MLP) with architecture $3072 \to 512 \to 256 \to 10$. Both use ReLU activations with no dropout or batch normalization (to maintain clean geometric structure).

\textbf{Training.} Models are trained on MNIST and CIFAR-10 under two regimes: (1) \textit{clean}, with ground-truth labels; (2) \textit{corrupted}, with 20\% of training labels randomly flipped. Training uses Adam ($\text{lr}=0.001$, $\text{epochs}=20$, $\text{batch}=128$). Xavier initialization is used throughout.

\textbf{Statistical methodology.} All results are reported across 10 seeds (MNIST) or 5 seeds (CIFAR-10) with 95\% bootstrap confidence intervals. Paired t-tests are used for clean-vs-corrupted comparisons.

\textbf{Separability metrics.} We use two class-separability metrics with distinct roles. The Fisher discriminant ratio (FDR = $\operatorname{tr}(S_B)/\operatorname{tr}(S_W)$) is our primary metric: it is dimension-invariant and does not conflate geometric spread with dimensionality, making it suitable for cross-width comparisons in Section~\ref{sec:scaling}. The Davies--Bouldin index is retained as an auxiliary diagnostic in the t-SNE visualization (Figure~\ref{fig:tsne}) to show geometric disentanglement across layers. Both are computed from hidden activations; FDR is the formally reported metric in Sections~\ref{sec:phase1} and~\ref{sec:scaling}.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{../outputs/figures/figure1_framework.png}
  \caption{
    Four-phase analysis framework. Phase~1 measures spectral geometry of weight matrices; Phase~2 computes CKA similarity across layer pairs; Phase~3 applies influence functions via conjugate gradient; Phase~4 performs rank-one model editing (ROME) and measures delta-norms.
  }
  \label{fig:framework}
\end{figure}

% ============================================================
\section{Phase 1: Weight Geometry}
\label{sec:phase1}

We first ask whether label memorization leaves a detectable trace in the singular value spectrum of the network's weight matrices. Table~\ref{tab:spectral_mnist} reports spectral and Frobenius norms for both layers on MNIST.

\begin{table}[t]
  \centering
  \caption{MNIST weight geometry (10 seeds, 95\% CI). All paired differences are significant ($p < 0.001$).}
  \label{tab:spectral_mnist}
  \begin{tabular}{lcc}
    \toprule
    Metric & Clean & Corrupted (20\%) \\
    \midrule
    Train Accuracy & $96.68\%\ [96.60\%,\ 96.77\%]$ & $94.21\%\ [94.02\%,\ 94.40\%]$ \\
    Test Accuracy & $95.31\%\ [95.16\%,\ 95.46\%]$ & $93.71\%\ [93.42\%,\ 94.00\%]$ \\
    FC1 Spectral Norm & $4.37\ [4.24,\ 4.50]$ & $3.66\ [3.53,\ 3.79]$ \\
    FC2 Spectral Norm & $2.58\ [2.42,\ 2.75]$ & $1.39\ [1.30,\ 1.47]$ \\
    FC1 Frobenius Norm & $12.56\ [12.38,\ 12.74]$ & $10.33\ [10.19,\ 10.47]$ \\
    FC2 Frobenius Norm & $4.99\ [4.83,\ 5.16]$ & $2.79\ [2.70,\ 2.87]$ \\
    Gradient Norm (final) & $0.94\ [0.89,\ 0.98]$ & $1.00\ [0.96,\ 1.05]$ \\
    \bottomrule
  \end{tabular}
\end{table}

Training accuracy drops from $96.68\%$ to $94.21\%$, while test accuracy drops from $95.31\%$ to $93.71\%$. Both spectral norms decrease under corruption (FC1: $4.37\to3.66$; FC2: $2.58\to1.39$). This flattening of the singular value spectrum indicates that label-noise models operate in a lower-effective-rank regime -- the weight matrix distributes its capacity more uniformly across singular directions.

On CIFAR-10 (Table~\ref{tab:spectral_cifar}), the pattern is depth-dependent. The input-layer spectral norm (FC1) is unchanged ($p=0.71$), while FC2 and FC3 increase significantly ($p=0.0003$ and $p=0.017$, respectively).

\begin{table}[t]
  \centering
  \caption{CIFAR-10 weight geometry (5 seeds, 95\% CI).}
  \label{tab:spectral_cifar}
  \begin{tabular}{lccc}
    \toprule
    Layer & Clean & Corrupted (20\%) & $p$-value \\
    \midrule
    FC1 Spectral Norm & $18.22\ [17.93,\ 18.51]$ & $18.31\ [17.93,\ 18.70]$ & $0.71$ \\
    FC2 Spectral Norm & $8.99\ [8.83,\ 9.15]$ & $9.98\ [9.85,\ 10.10]$ & $0.0003$ \\
    FC3 Spectral Norm & $1.72\ [1.68,\ 1.77]$ & $1.88\ [1.78,\ 1.98]$ & $0.017$ \\
    \bottomrule
  \end{tabular}
\end{table}

\textbf{Finding.} Spectral norm changes are depth-dependent. In the two-layer MNIST network both layers flatten, consistent with Proposition~\ref{prop:spectral}. In the three-layer CIFAR-10 network, the input layer is unaffected while output-adjacent layers intensify, consistent with deeper layers absorbing additional separability burden when resolving noisy labels.

% ============================================================
\section{Phase 2: Representation Analysis}
\label{sec:phase2}

While weight geometry measures the parameter space, representation similarity via CKA measures the functional consequence of memorization on hidden representations. We compute linear CKA between consecutive layer pairs (pre- and post-activation) for every layer boundary.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{../outputs/figures/figure3_tsne_manifold.png}
  \caption{
    t-SNE visualization of representations across input, hidden, and output layers for a clean MNIST model. The manifold transitions from digit-clustered (input) through compressed (hidden) to linearly separable (output).
  }
  \label{fig:tsne}
\end{figure}

Table~\ref{tab:cka_mnist} reports CKA for the three layer boundaries on MNIST.

\begin{table}[t]
  \centering
  \caption{MNIST CKA similarity (10 seeds, 95\% CI).}
  \label{tab:cka_mnist}
  \begin{tabular}{lccc}
    \toprule
    Layer Pair & Clean & Corrupted & $\Delta$ & $p$-value \\
    \midrule
    input$\to$fc1\_pre & $0.712\ [0.697,\ 0.727]$ & $0.693\ [0.676,\ 0.710]$ & $+0.019$ & $0.08$ \\
    \textbf{fc1\_pre$\to$fc1\_post} & $\mathbf{0.850\ [0.828,\ 0.873]}$ & $\mathbf{0.690\ [0.669,\ 0.712]}$ & $\mathbf{+0.160}$ & $\mathbf{<0.001}$ \\
    fc1\_post$\to$output & $0.706\ [0.666,\ 0.745]$ & $0.715\ [0.685,\ 0.745]$ & $-0.009$ & $0.65$ \\
    \bottomrule
  \end{tabular}
\end{table}

The ReLU nonlinearity (fc1\_pre$\to$fc1\_post) shows a dramatic CKA drop under corruption: $0.850 \to 0.690$ ($\Delta = +0.160$, $p < 0.001$). The linear transformations on either side are nearly identical. Memorization distortion localizes at the single nonlinearity.

On CIFAR-10 (Table~\ref{tab:cka_cifar}), the pattern is more nuanced.

\begin{table}[t]
  \centering
  \caption{CIFAR-10 CKA similarity (5 seeds, 95\% CI).}
  \label{tab:cka_cifar}
  \begin{tabular}{lccc}
    \toprule
    Layer Pair & Clean & Corrupted & $\Delta$ & $p$-value \\
    \midrule
    input$\to$fc1\_pre & $0.767$ & $0.765$ & $+0.002$ & $0.88$ \\
    fc1\_pre$\to$fc1\_post & $0.819$ & $0.801$ & $+0.018$ & $0.017$ \\
    fc1\_post$\to$fc2\_pre & $0.721$ & $0.749$ & $-0.028$ & $0.08$ \\
    fc2\_pre$\to$fc2\_post & $0.177$ & $0.140$ & $+0.037$ & $0.09$ \\
    \textbf{fc2\_post$\to$output} & $\mathbf{0.580}$ & $\mathbf{0.477}$ & $\mathbf{+0.103}$ & $\mathbf{0.009}$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{../outputs/figures/figure4_neuron_weights.png}
  \includegraphics[width=\textwidth]{../outputs/figures/figure5_correlation_heatmap.png}
  \caption{
    Top: Hidden neuron weight vectors reshaped as $28\times28$ images for interpretability. Bottom: Pearson correlation between each hidden neuron's class-conditional activation and the target labels.
  }
  \label{fig:neurons_corr}
\end{figure}

\textbf{Finding (CIFAR-10).} The largest CKA distortion is at the deepest pre-output interface -- fc2\_post$\to$output ($\Delta = +0.103$, $p = 0.009$). The first nonlinearity shows a much smaller effect ($\Delta = +0.018$, $p = 0.017$). This confirms Proposition~\ref{prop:nonlinearity}: distortion concentrates at the deepest pre-output interface. In a two-layer network this interface is the single ReLU; in a three-layer network it shifts to the output linear layer.

Corrupted models also show higher sparsity (0.357~$\to$~0.576) and substantially lower mean activations (4.83~$\to$~1.53), indicating that a larger fraction of hidden units are silent and active units carry weaker signals.

% ============================================================
\section{Phase 3: Influence Functions}
\label{sec:phase3}

Phase~3 measures whether memorized (corrupted) samples are genuinely harder to predict than clean samples. We use two complementary definitions: (i) a \emph{clean-model loss-quantile} definition (top 25\% by loss) as a methodological baseline, and (ii) a \emph{corrupted-model ground-truth} definition where corruption indices directly identify memorized samples.

\begin{table}[t]
  \centering
  \caption{Influence function results (10 seeds, 95\% CI).}
  \label{tab:phase3}
  \begin{tabular}{lcc}
    \toprule
    Metric & Clean Model & Corrupted Model \\
    \midrule
    Definition & Loss-quantile (top 25\%) & Ground-truth (20\% flipped) \\
    Mean Loss & $0.112\ [0.108,\ 0.117]$ & $0.408\ [0.396,\ 0.420]$ \\
    Loss Gap (memorized $-$ forgotten) & $+0.442\ [0.426,\ 0.458]$ & $\mathbf{-0.051\ [-0.057,\ -0.045]}$ \\
    \bottomrule
  \end{tabular}
\end{table}

\textbf{Interpretation.} The clean model's loss-quantile analysis is circular by construction: defining memorized samples as the top 25\% by loss guarantees a positive gap. This is provided only as a methodological baseline. The genuine finding is the corrupted model's non-circular result: using ground-truth corruption indices breaks the circularity and reveals a gap of $-0.051$ (CI excludes zero). This confirms that corrupted (memorized) samples are reliably harder to predict than clean samples -- the model has learned the true labels better than the flipped ones.

As an auxiliary validation, we compute gradient alignment -- the cosine similarity between gradients from genuine and corrupted training samples at the output layer. At convergence, alignment is near-perfect ($+0.9944\ [0.9926,\ 0.9961]$), consistent with the model treating both groups of samples as equally valid training signals. The gradient signal corroborates the loss-based finding from a distinct geometric perspective.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{../outputs/figures/figure6_circuit_map.png}
  \caption{
    Circuit map showing critical neuron-class connections. Each row is a hidden neuron; each column is an output class. Darker entries indicate stronger contributions.
  }
  \label{fig:circuit}
\end{figure}

% ============================================================
\section{Phase 4: ROME as a Memorization Localizer}
\label{sec:phase4}

We apply rank-one model editing (ROME)~\cite{meng2022locating} to measure how easily individual class predictions can be changed with a single rank-one update to the weight matrix. The ROME delta-norm $\|\Delta\|$ serves as a geometric probe for class boundary overlap: larger edits correspond to more orthogonal class boundaries, smaller edits to overlapping ones (Proposition~\ref{prop:rome}).

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{../outputs/figures/figure2_rome_pipeline.png}
  \caption{
    ROME pipeline: (1) select source class $s$ and target class $t$; (2) compute hidden representations for $s$; (3) compute edit $\Delta$ via the rank-one update formula; (4) measure accuracy recovery on source class $s$ after applying the edit.
  }
  \label{fig:rome_pipeline}
\end{figure}

Table~\ref{tab:rome_mnist} reports per-class delta-norms for both layers across 10 seeds.

\begin{table}[t]
  \centering
  \caption{MNIST ROME delta-norms (10 seeds, all classes, all $p<0.0001$).}
  \label{tab:rome_mnist}
  \begin{tabular}{lcc}
    \toprule
    Layer & Clean $\Delta$-norm (avg) & Corrupted $\Delta$-norm (avg) & Ratio \\
    \midrule
    FC1 & 14.49 & 7.18 & $2.02\times$ \\
    FC2 & 19.08 & 4.40 & $4.34\times$ \\
    \bottomrule
  \end{tabular}
\end{table}

Every individual class shows clean $>$ corrupted at $p<0.0001$ for both fc1 and fc2. This is the strongest statistical result in the paper -- 10 classes $\times$ 2 layers $\times$ 10 seeds with uniformly significant differences.

On CIFAR-10 (Table~\ref{tab:rome_cifar}), all 10 classes confirm the pattern at $p<0.05$ with a mean ratio of $1.84\times$.

\begin{table}[t]
  \centering
  \caption{CIFAR-10 ROME delta-norms (fc3, 3 seeds).}
  \label{tab:rome_cifar}
  \begin{tabular}{lccc}
    \toprule
    Class & Clean $\Delta$ (mean $\pm$ SEM) & Corrupted $\Delta$ (mean $\pm$ SEM) & Ratio & $p$-value \\
    \midrule
    0 & $0.325 \pm 0.035$ & $0.175 \pm 0.027$ & $1.85\times$ & 0.028 \\
    1 & $0.438 \pm 0.033$ & $0.262 \pm 0.018$ & $1.68\times$ & 0.009 \\
    2 & $0.385 \pm 0.027$ & $0.186 \pm 0.016$ & $2.07\times$ & 0.003 \\
    3 & $0.341 \pm 0.009$ & $0.196 \pm 0.015$ & $1.74\times$ & 0.001 \\
    4 & $0.339 \pm 0.019$ & $0.169 \pm 0.017$ & $2.01\times$ & 0.003 \\
    5 & $0.376 \pm 0.012$ & $0.214 \pm 0.007$ & $1.76\times$ & $<0.001$ \\
    6 & $0.327 \pm 0.014$ & $0.170 \pm 0.005$ & $1.93\times$ & $<0.001$ \\
    7 & $0.408 \pm 0.016$ & $0.216 \pm 0.006$ & $1.89\times$ & $<0.001$ \\
    8 & $0.366 \pm 0.037$ & $0.197 \pm 0.036$ & $1.85\times$ & 0.030 \\
    9 & $0.380 \pm 0.028$ & $0.237 \pm 0.019$ & $1.60\times$ & 0.013 \\
    \bottomrule
  \end{tabular}
\end{table}

The cross-architecture consistency -- ratio $\sim2$--$4\times$ on MNIST and $\sim1.8\times$ on CIFAR-10, across different depths, widths, and datasets -- suggests the relationship between class boundary overlap and edit magnitude is an architectural invariant.

\paragraph{Noise rate sweep.} We next test whether the ROME delta-norm ratio scales monotonically with label noise rate. Table~\ref{tab:noise_sweep} shows clean-vs-corrupted ratios at 10\%, 20\%, and 40\% noise.

\begin{table}[t]
  \centering
  \caption{ROME delta-norm scales monotonically with noise rate (MNIST fc2, 5 seeds).}
  \label{tab:noise_sweep}
  \begin{tabular}{lcc}
    \toprule
    Noise Rate & FC2 $\Delta$-norm & Ratio vs Clean \\
    \midrule
    0\% (Clean) & 19.08 & 1.00$\times$ \\
    10\% & $\sim$5.67 & 3.37$\times$ \\
    20\% & $\sim$4.40 & 4.34$\times$ \\
    40\% & $\sim$3.19 & 5.98$\times$ \\
    \bottomrule
  \end{tabular}
\end{table}

The ratio increases monotonically from 3.37$\times$ to 5.98$\times$, confirming that delta-norm tracks the degree of memorization continuously rather than being a binary clean-vs-corrupted artifact.

\paragraph{Baseline comparison.} We compare ROME against two simpler memorization probes: (i) the spectral norm ratio from Phase~1, and (ii) a linear probe trained on hidden activations to predict whether a sample is corrupted. The spectral norm ratio is 1.86$\times$ (fc2: 2.58/1.39), which ROME outperforms by 2.3$\times$ (4.34/1.86). The linear probe achieves AUC of only 0.514 across 5 seeds, barely above random (0.5). This rules out the hypothesis that hidden activations trivially encode corruption status -- ROME captures a geometric property (class boundary overlap in weight space) that these simpler methods cannot access.

\paragraph{Random baseline.} To verify that ROME's recovery is not an artifact of any rank-one perturbation, we compare against random rank-one edits: replacing the ROME-computed $\Delta$ with a random matrix of equal norm. Across all four targeted-corruption configurations, random edits recover 0\% of lost accuracy in every seed, yielding a signal ratio of $\infty$ (ROME recovery 10--22\% vs~random 0\%). This confirms that ROME's edit direction -- not just its magnitude -- is mechanistically meaningful.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{../outputs/figures/figure7_rome_recovery.png}
  \caption{
    ROME accuracy recovery for four targeted corruption configurations. Recovery is positive in all 4 configs (7$\to$1, 1$\to$7, 5$\to$6, 0$\to$8) with consistent side effects.
  }
  \label{fig:rome_recovery}
\end{figure}

\textbf{Multi-class targeted corruption.} We next measure whether ROME can \emph{repair} corrupted predictions by editing only the output layer. Recovery is positive in all 4 targeted corruption configs (7$\to$1: $+0.141$, 1$\to$7: $+0.217$, 5$\to$6: $+0.120$, 0$\to$8: $+0.097$ across 5 seeds, mean recovery 14.4\%) with per-config side effects of 17.1\%, 24.0\%, 24.6\%, and 21.6\% respectively. The 0$\to$8 case, previously reported as $+0.014$ from a single seed, averages $+0.097$ across 5 seeds -- the earlier value reflected seed variance, not a systematic failure.

\paragraph{Multi-layer ROME.} Because memorization under targeted corruption is distributed across both weight layers (fc2 edits on 7$\to$1 recover 14.1\%; fc1 edits alone recover 0\% for all configs), we test sequential multi-layer editing: applying ROME to fc2 first, then to fc1. Sequential editing improves recovery across all configs, from 10--22\% (single-layer) to 22--40\% (multi-layer), an improvement of approximately 50\%. This confirms the distributed nature of memorization and demonstrates that multi-layer interventions can more completely reverse the backdoor effect.

% ============================================================
\section{Scaling Analysis}
\label{sec:scaling}

We train MNIST models with hidden dimensions 16--1024 and measure how memorization-related metrics scale with width.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{../outputs/figures/figure8_scaling_analysis.png}
  \caption{
    Scaling analysis across hidden dimensions 16--1024. Top-left: FDR (Fisher discriminant ratio, tr(S$_B$)/tr(S$_W$)). Top-right: Monosemanticity (fraction of neurons with $\rho>0.5$). Bottom-left: Mean circuit size (neurons/class). Bottom-right: Activation sparsity. Sparsity converges to 1.0 beyond $h=128$.
  }
  \label{fig:scaling}
\end{figure}

\begin{table}[t]
  \centering
  \caption{Scaling metrics across hidden dimensions (10 seeds).}
  \label{tab:scaling}
  \begin{tabular}{lcccc}
    \toprule
    Hidden Dim & Monosemanticity & Circuit Size & Sparsity & Accuracy \\
    \midrule
    16 & 0.269 & 6.6 & 0.588 & 95.31\% \\
    32 & 0.222 & 8.3 & 0.740 & 96.85\% \\
    64 & 0.212 & 3.5 & 0.945 & 97.50\% \\
    128 & 0.184 & 0.4 & 0.997 & 97.85\% \\
    256 & 0.175 & 0.1 & 1.000 & 97.93\% \\
    512 & 0.154 & 0.0 & 1.000 & 98.17\% \\
    1024 & 0.075 & 0.0 & 1.000 & 98.11\% \\
    \bottomrule
  \end{tabular}
\end{table}

\textbf{Finding.} As width increases beyond 64 hidden neurons, monosemanticity decreases monotonically (0.269 $\to$ 0.075) -- wider models distribute representations, reducing neuron specialization. Sparsity converges to 1.0 beyond $h=128$, meaning a vanishing fraction of capacity is used per class. This superlinear compression aligns with the superposition hypothesis \cite{elhage2022toy}: larger networks can pack more features per dimension, rendering individual circuit elements less specialized. Accuracy plateaus at $\sim$98\% beyond $h=256$.

% ============================================================
\section{Discussion and Conclusion}
\label{sec:discussion}

We presented a systematic four-phase analysis of label memorization in shallow ReLU networks, combining spectral geometry, CKA similarity, influence functions, and ROME rank-one editing. Our central finding is that memorization leaves detectable structural fingerprints that concentrate at the deepest pre-output interface, with cross-architecture consistency validated on both MNIST and CIFAR-10.

\textbf{Summary of findings.} (1) Spectral geometry changes are depth-dependent. On MNIST (2-layer), both FC1 and FC2 spectral norms decrease under corruption (4.37$\to$3.66 and 2.58$\to$1.39). On CIFAR-10 (3-layer), FC1 is unchanged (p=0.71) while FC2 and FC3 increase (p=0.0003, p=0.017), consistent with deeper layers absorbing additional separability burden. (2) CKA analysis reveals that memorization distortion localizes at the deepest pre-output interface: the ReLU nonlinearity on MNIST ($\Delta=+0.160$, $p<0.001$) and the output linear layer on CIFAR-10 ($\Delta=+0.103$, $p=0.009$). This pattern is predicted by our Proposition~1. (3) Our non-circular loss-based influence analysis confirms that corrupted samples are reliably harder to predict than clean ones ($-0.051$ loss gap, CI excluding zero). (4) ROME delta-norms serve as a robust cross-architecture separability probe: corrupted models require smaller rank-one perturbations on both MNIST and all 10 CIFAR-10 classes ($p<0.05$). The ROME ratio scales monotonically with noise rate (3.37$\times$ at 10\% to 5.98$\times$ at 40\%) and outperforms both spectral norm ratios (1.86$\times$) and linear probes (AUC 0.514). (5) Wider networks distribute memorized associations across more neurons, reducing monosemanticity and aligning with the superposition hypothesis of Elhage et al.~\cite{elhage2022toy}.

\textbf{Limitations.} Several caveats merit explicit acknowledgement. First, the primary MNIST experiments use a single architecture ($784\to16\to10$) with only 16 hidden neurons. While our CIFAR-10 validation partially addresses generalization concerns, the spectral geometry finding is depth-dependent rather than universal — a result that must be reported neutrally. The circuit size metric at $h=16$ has almost no headroom, and the scaling experiment (where circuit size converges to zero at $h=128+$) is the more meaningful result; we emphasize the scaling findings over the $h=16$ circuit measurements. Relatedly, the width-scaling analysis is correlational rather than causal: while FDR, monosemanticity, and sparsity all vary with width, the full four-phase pipeline was not run at each width, so we cannot conclude whether ROME recovery or CKA distortion scale monotonically with capacity. Second, our single-layer ROME edits, while reliable as detectors across 10 seeds (all $p<0.0001$), do not fully repair corrupted predictions — recovery is positive in all 4 targeted corruption configs (range $+0.097$ to $+0.217$) but partial. Sequential multi-layer ROME improves recovery to 22--40\%, yet even this remains incomplete, suggesting that a fully corrective intervention may require optimizing edits jointly rather than sequentially. Third, the influence function approximation uses conjugate gradient with finite iterations; our sensitivity analysis shows the loss gap is stable across 10--50 CG iterations, but the approximation remains inexact. Fourth, all experiments are on image classification with shallow fully-connected networks; extension to Transformers and language domains is open future work.

\textbf{Future work.} Three directions are immediate. (i) Joint multi-layer ROME: optimizing the edit across all layers simultaneously (rather than sequentially) to determine whether recovery is additive or subject to diminishing returns. (ii) Depth variation studies: testing whether the deepest pre-output interface localization finding holds across a wider range of depths (4--10 layers) where multiple nonlinearities may share the distortion burden. (iii) Connection to privacy: if memorization leaves detectable spectral and representational fingerprints, these signatures could serve as targets for selective forgetting in machine unlearning applications.

% ============================================================
% Bibliography
\bibliographystyle{unsrt}
\bibliography{related_work}

% ============================================================
\appendix
\section{Rank Ablation}
\label{sec:rank_ablation}

We ablate the SVD rank of the MNIST fc2 weight matrix to measure how effectively each model class compresses its task-relevant information into the top singular directions.

\begin{table}[h]
  \centering
  \caption{Accuracy at each SVD rank of fc2 (10 seeds, 95\% CI).}
  \label{tab:rank_ablation}
  \begin{tabular}{lcc}
    \toprule
    Rank & Clean & Corrupted (20\%) \\
    \midrule
    1/10 & 19.0\% [17.9\%, 20.1\%] & 19.3\% [16.6\%, 21.9\%] \\
    2/10 & 33.9\% [30.6\%, 37.2\%] & 30.0\% [26.4\%, 33.6\%] \\
    3/10 & 44.9\% [40.2\%, 49.7\%] & 40.5\% [37.0\%, 44.0\%] \\
    4/10 & 58.3\% [53.5\%, 63.2\%] & 53.2\% [49.0\%, 57.3\%] \\
    \textbf{5/10} & \textbf{68.4\% [62.2\%, 74.6\%]} & \textbf{62.3\% [57.9\%, 66.7\%]} \\
    6/10 & 75.4\% [67.3\%, 83.5\%] & 72.6\% [68.0\%, 77.3\%] \\
    7/10 & 80.1\% [70.8\%, 89.4\%] & 80.7\% [76.3\%, 85.0\%] \\
    8/10 & 88.9\% [85.7\%, 92.2\%] & 86.4\% [84.0\%, 88.8\%] \\
    9/10 & 92.8\% [90.7\%, 94.9\%] & 92.3\% [91.8\%, 92.9\%] \\
    10/10 (full) & 95.3\% [95.2\%, 95.5\%] & 93.7\% [93.4\%, 94.0\%] \\
    \bottomrule
  \end{tabular}
\end{table}

The key comparative signal is at rank 5/10: corrupted models perform 6.1pp worse than clean (62.3\% vs 68.4\%). This gap is intermediate-rank-specific -- at rank 8/10 both models reach $\sim$93\% of full accuracy, and at full rank the gap is only 1.6pp. The rank-5 finding shows that corrupted models require a broader effective rank spectrum: their memorized label-noise associations are less compressible into the top singular directions. This complements the Phase~1 finding that MNIST spectral norms decrease under corruption, confirming a flatter singular value distribution.

\end{document}
